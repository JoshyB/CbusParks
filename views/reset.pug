extends layout

block content
  .inner
    form(method="POST")
      h2 #{ title } 
      label(for="password") Password
      input(type="password" name="password")
      label(for="password-confirm") Confrim Password
      input(type="password" name="password-confirm")
      input(type="submit" value="Reset my password" class="button")